codex:
  cwd: .
  role:  # optional — e.g. "implementation and tests" — assign per project
  always_read: [ROUTING.md, README.md]
  startup: The trusted pneu SessionStart hook automatically queues and validates the current Codex thread binding; normally, do not bind manually. Use the fallback only when pneu diagnostics explicitly report that automatic binding did not complete: `~/.pneu/bin/rt-codex-wake bind "$RT_PROJECT_ROOT"`
